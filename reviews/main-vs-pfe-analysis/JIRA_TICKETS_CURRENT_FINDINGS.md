# Jira Tickets for Current Findings

This document converts the confirmed findings from the `main` vs `pfe` review into a table-oriented Jira backlog.

## Ticket Table

| Ticket | Repo | Type | Priority | Title | Problem | Evidence | Suggested Fix | Acceptance Criteria |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `osm-sec` | Bug | High | Roll back account activation flow when OTP email delivery fails | The account activation flow persists a disabled user and activation OTP before sending the email. If email delivery fails, the database can be left in a partial state because checked exceptions may not trigger transaction rollback. | [UserService.java:141](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:141>), [UserService.java:154](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:154>), [UserService.java:166](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:166>) | Add explicit rollback behavior such as `rollbackFor = Exception.class`, or move mail delivery to an async/retryable flow and track notification status explicitly. | Activation email failure must not leave an unrecoverable partial registration. Registration retries must behave predictably, failure must be logged clearly, and the failure path must be covered by tests. |
| 2 | `osm-sec` | Bug | High | Roll back or recover password reset flow when reset email delivery fails | The password reset flow stores the reset OTP before email delivery. If the email send fails, a valid reset code can remain server-side without the user receiving it. | [UserService.java:347](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:347>), [UserService.java:366](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:366>), [UserService.java:370](</f:/OSM PROJECT/osm-sec/src/main/java/com/osm/securityservice/userManagement/service/UserService.java:370>) | Apply explicit rollback rules if mail stays inline, or move reset notification delivery to an async/retryable channel. Ensure repeated reset requests safely replace or invalidate prior reset codes. | Reset mail failure must not leave a broken partial reset state. Subsequent reset requests must still work, logs must identify the failure, and tests must cover the failure and retry path. |
| 3 | `osm-ms-fe` | Bug | High | Replace hardcoded localhost API URLs with environment-based configuration | Several frontend services call hardcoded localhost URLs directly instead of using shared environment config, making the app fragile outside one local setup. | [environment.ts:10](</f:/osm-ms-fe/src/environments/environment.ts:10>), [QualityService.ts:11](</f:/osm-ms-fe/src/app/OF/services/QualityService.ts:11>), [BomService.ts:11](</f:/osm-ms-fe/src/app/stock/services/BomService.ts:11>), [sku.service.ts:11](</f:/osm-ms-fe/src/app/stock/services/sku.service.ts:11>), [stock.service.ts:13](</f:/osm-ms-fe/src/app/stock/services/stock.service.ts:13>) | Refactor all affected services to derive API URLs from `environment.apiUrl`. Optionally centralize API route prefixes in a shared config helper and scan the repo for unintended `localhost` usage. | No feature service should hardcode backend localhost URLs unless explicitly documented. Dev and prod switching must work through environment config only, and existing modules must continue working after the refactor. |
| 4 | `osm-ms-fe` | Bug | High | Fix stock statistics service using wrong backend port | The stock statistics service points to `http://localhost:8080` while the app environment and other frontend services use `http://localhost:8084`, which likely causes runtime failure in the statistics views. | [statistique.service.ts:10](</f:/osm-ms-fe/src/app/stock/services/statistique.service.ts:10>), [environment.ts:10](</f:/osm-ms-fe/src/environments/environment.ts:10>) | Replace the hardcoded `8080` URL with an environment-derived base URL. If a separate backend is intentional, expose that via documented environment config instead of a hardcoded port. | Statistics API calls must resolve correctly in the same environment as the rest of the stock module. No unexplained hardcoded port should remain, and stock statistics pages must load successfully in target environments. |
| 5 | `osm-mobile` | Bug | Medium | Remove emulator-only API base URL from `pfe` branch | The mobile branch uses `http://10.0.2.2:8084/` as its base URL. That works on Android emulators but not on physical devices. | `origin/pfe:app/src/main/java/com/xdev/osm_mobile/utils/Constants.kt:5` | Move the base URL into build flavors, build config, or environment-specific constants for emulator, LAN/device testing, and production. | The app must target backend environments without code edits. Emulator and physical-device testing must both work, and no release-target branch should default to an emulator-only base URL. |
| 6 | `osm-prod` / `osm-pack` | Bug | Medium | Replace generic RuntimeExceptions with specific domain exceptions | Services like `FiltrationService` and `BomService` swallow domain exceptions and rethrow generic `RuntimeException`, erasing stack traces and breaking global API error handling. | `FiltrationService.java:125`, `BomService.java:47` | Implement custom exceptions (e.g. `ResourceNotFoundException`) and a global `@ControllerAdvice` to map them to appropriate HTTP codes. | API errors return correct HTTP codes (404, 400) with clear messages, no `500 Internal Server Error` for bad requests. |
| 7 | All Microservices | Tech Debt | High | Resolve "DTO Split-Brain" by moving shared DTOs to `osm-parent` | DTOs (like `StorageUnitDto`, `BonCommandeDto`) are duplicated across microservices instead of being centralized, causing serialization risks during Feign client communication. | `osm-prod/dto/StorageUnitDto.java`, `osm-pack/dto/BonCommandeDto.java` | Create an `osm-shared-dto` module inside `osm-parent` and migrate all externally facing DTOs there. | No duplicate DTOs exist across microservices. Feign clients import DTOs directly from `osm-parent`. |
| 8 | `osm-ms-fe` | Bug | High | Reactivate or clean up the dead Stock Dashboard | The entire logic in `stock-dashboard.component.ts` is commented out, leaving the dashboard feature completely disabled and broken in the `pfe` branch. | `stock-dashboard.component.ts:1-179` | Uncomment the logic, ensure the API endpoints are reachable, and fix any compilation errors that caused the developer to comment it out. | The Stock Dashboard loads successfully with active charts and data. |
| 9 | `osm-ms-fe` | Tech Debt | Medium | Fix Angular Memory Leaks (Missing RxJS Unsubscribes) | New components (e.g. `ArticleListComponent`, `StockParEmplacementComponent`) subscribe to Observables but never implement `OnDestroy` to unsubscribe, leading to memory leaks over time. | `article-list.component.ts:37`, `stock-par-emplacement.component.ts:41` | Implement `ngOnDestroy` with `Subject.next()` and use the `takeUntil` RxJS operator, or use Angular's `AsyncPipe` in the HTML templates. | All component subscriptions are properly cleaned up upon component destruction. |
| 10 | Frontend & Backend | Security | Critical | Implement Missing RBAC Authorization (Guards & PreAuthorize) | New modules in the `pfe` branch (`stock`, `hr`, etc.) have no Angular Auth Guards (`canActivate`), and their corresponding backend controllers lack `@PreAuthorize` annotations. | `stock-routing.module.ts`, `BomController.java` | Add `anyPermissionGuard` to frontend routes and `@PreAuthorize("hasAuthority('...')")` to backend controllers to enforce Role-Based Access Control. | Users can only access UI routes and API endpoints for which they have explicit permissions. |
| 11 | Frontend & Backend | Tech Debt | Low | Standardize Logging (Enforce LoggingUtils and remove console.log) | Developers used `System.out.println` and `e.printStackTrace()` in the backend, and left hundreds of `console.log` statements in the frontend production code. | `SeuilAlerteService.java`, `StatistiqueService.java`, various `.ts` files | Strictly replace all Java console prints with the project's custom `LoggingUtils` utility (e.g., `LoggingUtils.logMethodEntry`, `LoggingUtils.logError`) and remove/disable Angular `console.log` statements for production builds. | Logs are properly formatted, leveled, and securely handled by the core logging utility without leaking debug data to the client console. |
| 12 | `osm-ms-fe` | Tech Debt | Medium | Standardize UI/UX and Layout Responsiveness | Frontend components duplicate hundreds of lines of SCSS, lack data table pagination, have broken mobile responsiveness, and use inconsistent action menus. | `article-list.component.html/scss`, `fournisseur-list.component.html/scss` | Extract shared SCSS into global styles or shared components, add client-side/server-side pagination to tables, use Bootstrap grids for responsive filters, and standardize dropdowns. | The UI is consistent, performant with large datasets, and responsive on mobile devices without duplicated CSS. |
| 13 | `osm-mobile` | Security | Critical | Secure Hardcoded Secrets and Plaintext Tokens | The mobile app hardcodes `CLIENT_SECRET` and `QR_SECRET` in constants, stores JWT tokens in plaintext `SharedPreferences`, and leaks tokens in Logcat via `HttpLoggingInterceptor.Level.BODY`. | `Constants.kt`, `SessionManager.kt`, `RetrofitClient.kt` | Move secrets to `BuildConfig`/server-side, use `EncryptedSharedPreferences` for tokens, and disable body logging in release builds. | The mobile app safely stores credentials and does not expose secrets to reverse engineering or Logcat snooping. |
| 14 | `osm-mobile` | Tech Debt | Medium | Implement MVVM Architecture and Extract UI Resources | The Android app mixes business logic (sync, network) directly inside Activities, and hardcodes UI strings and dimensions in XML layouts and Kotlin files. | `MainActivity.kt`, `activity_main.xml` | Refactor Activities to use `ViewModel` and `Repository` patterns. Extract all hardcoded text to `strings.xml`. | The mobile app follows modern Android architecture, is easier to test, and supports full internationalization. |
## Suggested Epic Structure

| Epic | Story Group | Included Tickets |
| --- | --- | --- |
| `PFE Branch Stabilization` | Authentication and User Lifecycle Stability | 1, 2 |
| `PFE Branch Stabilization` | Frontend Environment and API Configuration Cleanup | 3, 4 |
| `PFE Branch Stabilization` | Mobile Environment Configuration Hardening | 5 |
| `PFE Branch Stabilization` | Backend Exception Handling Standardization | 6 |
| `Architecture & Refactoring` | Centralize Shared DTOs (Resolve Split-Brain) | 7 |
| `PFE Branch Stabilization` | Frontend Feature Restoration & Memory Leak Fixes | 8, 9 |
| `Security Hardening` | Enforce Role-Based Access Control (RBAC) | 10 |
| `Security Hardening` | Mobile Security & Token Management | 13 |
| `Quality & Observability` | Standardize Application Logging | 11 |
| `Architecture & Refactoring` | Standardize Frontend UI/UX and Layouts | 12 |
| `Architecture & Refactoring` | Mobile MVVM Architecture & UI Standards | 14 |

## Notes

| Topic | Detail |
| --- | --- |
| Review basis | Confirmed findings from the first pass only |
| Not yet expanded | Further review required for `osm-parent` shared utilities |
| Validation limitation | Maven validation could not be run because `mvn` is not installed in the current environment |
